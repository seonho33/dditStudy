<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.testBoard.mapper.ITestBoardMapper">

<insert id="registerBoard" parameterType="kr.or.ddit.vo.crud.Board" useGeneratedKeys="true">
	<selectKey keyProperty="boardNo" resultType="int" order="BEFORE">
		select seq_board.nextval from dual
	</selectKey>
	insert into board(
		BOARD_NO, 
		TITLE, 
		CONTENT, 
		WRITER, 
		REG_DATE
	)values(
		#{boardNo},
		#{title},
		#{content},
		#{writer},
		sysdate
	)
</insert>

<select id="getList" resultType="kr.or.ddit.vo.crud.Board">
	select	board_no,
			title,
			content,
			writer,
			reg_date
	from	board
	order by reg_date desc
</select>

<select id="searchList" parameterType="kr.or.ddit.vo.test.TestSearchDTO" resultType="kr.or.ddit.vo.crud.Board">
	select *

</select>


</mapper>