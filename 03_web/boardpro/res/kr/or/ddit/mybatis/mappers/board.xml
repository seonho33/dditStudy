<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="board">

<sql id="dynamicCondition">
   <where>
      <if test="!(svo.stype == '' or svo.sword == '')">
           ${svo.stype} like  '%'||#{svo.sword}||'%'
           <!--$기호는 컬럼명 #기호는 변수  -->
       </if>
   </where>
</sql>

<!-- 게시글 리스트 가져오기 3개씩 -->
<select id="pageByList" parameterType="pinfo" resultType="bvo">
 select A.* from 
     (select rownum as rnum , B.* from
        ( select * from boardtab
          <include refid="dynamicCondition"/> 
               order by  num desc ) B 
    <![CDATA[
          where rownum <= #{end}) A
       where A.rnum >= #{start} 
     ]]>            
 </select>
 
 
 <!-- 전체 글 갯수 구하기 -->
 <select id="getListCount" parameterType="pinfo" resultType="int">
  SELECT COUNT(*)
  FROM BOARDTAB
  <include refid="dynamicCondition"/> 
 
 </select>
 
 	<insert id="boardInsert" parameterType="bvo">
	
	INSERT INTO BOARDTAB(
		NUM,
		SUBJECT,
		WRITER,
		MAIL,
		PASSWORD,
		CONTENT,
		HIT,
		WIP,
		WDATE
	)
	VALUES(
		BOARDTAB_NUM_SEQ.NEXTVAL,
		#{subject},
		#{writer},
		#{mail},
		#{password},
		#{content},
		0,
		#{wip},
		SYSDATE
	
	)
	</insert>
 
 <!-- 조회수 증가 -->
 
 <update id="hitUpdate" parameterType="int">
 	update boardtab set hit = hit+1
 	where	num = #{data}
 </update>
 
</mapper>
