<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">
	
<!--
	이 영역에 SQL문에 맞는 태그를 사용하여 SQL문을 기술한다.
	
	사용할 수 있는 기본적인 태그들
	
	<insert> ~~~ </insert>
	<update> ~~~ </update>
	<delete> ~~~ </delete>
	<select> ~~~ </select>
	
	위 태그에서 사용되는 속성들
	1) id : 해당 태그를 호출할 때 namespace 속성과 연결하여 사용하는 쿼리 ID값
	
	2) parameterType : SQL문에 사용할 데이터가 들어있는 객체타입을 지정한다.
				(보통 VO클래스, 자바의 기본자료형이름 등이 사용된다.)
				(typeAlias 로 지정한 별명을 사용할 수 있다.)
				
	3) resultType : select문을 실행한 결과를 담을 객체타입을 지정한다.
				(보통 VO클래스, 자바의 기본자료형이름 등이 사용된다.)
				(typeAlias 로 지정한 별명을 사용할 수 있다.)
	
	4) resultMap : 결과 레코드컬럼명과 VO객체의 property명이 다를 경우에
				   적절한 매핑을 위한 정보 제공을 위해 사용한다.

  -->
<!-- 
	<insert id="insertMember" parameterType="kr.or.ddit.member.vo.MemberVO">
	
		INSERT INTO MYMEMBER
		(MEM_ID,MEM_NAME,MEM_TEL,MEM_ADDR)
		VALUES (#{memId},#{memName},#{memTel},#{memAddr})
	
	</insert>
	
	<update id="updateMember" parameterType="kr.or.ddit.member.vo.MemberVO">
		
		UPDATE MYMEMBER 
		SET	MEM_Name = #{memName},
			MEM_TEL = #{memTel},
			MEM_Addr = #{memAddr} 
		where mem_id = #{memId}
	
	</update>

	<delete id="deleteMember">
		
		DELETE FROM MYMEMBER 
		WHERE mem_id = #{e}
		
	</delete>
	
	<select id="getAllMember" resultType="mv">
		select	mem_id AS memId,
				mem_name AS memName,
				mem_tel AS memTel,
				mem_addr AS memAddr,
				reg_dt AS regDt
		from MYMEMBER
	</select>
	
	<select id="getMember" resultType="HashMap">
		select	*		
		from 	MYMEMBER 
		where	 mem_id = #{a}
	</select>
	
	<select id="checkMember" resultType="int">
			select count(*) as cnt from mymember
			where mem_id = #{memId}
	</select>
	
	<resultMap type="mv" id="memMap">
		<result column="mem_id" property="memId" />
		<result column="mem_name" property="memName" />
		<result column="mem_tel" property="memTel" />
		<result column="mem_addr" property="memAddr" />
		<result column="reg_dt" property="regDt" />
	</resultMap>
	
	<select id="searchMember" resultMap="memMap">
		select * from MYMEMBER
		<where>
			<if test='memId !=null and memId != ""'>
				and mem_id = #{memId}			
			</if>
			<if test='memName !=null and memName != ""'>
				and mem_name = #{memName}			
			</if>
			<if test='memTel !=null and memTel != ""'>
				and mem_tel = #{memTel}			
			</if>
			<if test='memAddr !=null and memAddr != ""'>
				and mem_addr like '%'||#{memAddr}||'%'		
			</if>
		</where>
	</select>
	 -->
	 
	<select id="memberList" resultType="mvo">
		select * from MEMBER
	</select>
	
	<!-- 로그인 -->
	<select id="loginMember" parameterType="mvo" resultType="mvo">
		SELECT *
		FROM MEMBER
		WHERE MEM_ID=#{mem_id} AND MEM_PASS = #{mem_pass}
	</select>
	
	<!-- 아이디 중복검사 -->
	<select id="checkId" parameterType="String" resultType="String">
		SELECT	MEM_ID
		FROM	MEMBER
		WHERE	MEM_ID=#{mem_id}
	</select>
	
	<!-- 우편번호 검색 -->
	<select id="dongBySelect" parameterType="String" resultType="zvo">
		SELECT	*
		FROM	ZIPTB
		WHERE	DONG LIKE #{dong}||'%'
	</select>
	
	
	
	<!-- 가입하기 -->
	<insert id="insertMember" parameterType="mvo">
	INSERT INTO MEMBER(
					    MEM_NAME,
					    MEM_ID,
					    MEM_PASS,
					    MEM_BIR,
					    MEM_ZIP,
					    MEM_ADD1,
					    MEM_ADD2,
					    MEM_HP,
					    MEM_MAIL
    ) 			VALUES(
					    #{mem_name},
					    #{mem_id},
					    #{mem_pass},
					    #{mem_bir},
					    #{mem_zip},
					    #{mem_add1},
					    #{mem_add2},
					    #{mem_hp},
					    #{mem_mail}
	)
	</insert>
	
	<insert id="insertUser" parameterType="uvo">
	INSERT INTO USERS(
		USER_ID,
		PASSWORD,
		NAME
	)	
	VALUES(
		#{userid},
		#{password},
		#{name}
	)
	</insert>
	
	<insert id="insertmember1" parameterType="mvo1">
	INSERT INTO MEMBER(
		USER_ID,
		USER_BIR,
		USER_MAIL,
		USER_NO
	)
	VALUES(
		#{userid},
		#{userbir},
		#{usermail},
		#{userno}
	)
	
	</insert>
	
	<insert id="boardInsert" parameterType="bvo">
	
	INSERT INTO BOARDTABLE(
		NUM      ,
		SUBJECT  ,
		WRITER   ,
		MAIL     ,
		PASSWORD ,
		CONTENT  ,
		HIT      ,
		WIP      ,
		WDATE
	)
	VALUES(
		BOARDTAB_NUM_SEQ.NEXTVAL,
		#{subject}  ,
		#{writer}   ,
		#{mail}     ,
		#{password} ,
		#{content}  ,
		0      ,
		#{wip}      ,
		SYSDATE
	
	)
	
	</insert>
	
	
	
</mapper>
