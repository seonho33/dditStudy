<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.domain.apt.board.ann.mapper.IAptAnnBoardPostMapper">

    <!-- 최신글 'limit' 만큼 가져오기 -->
    <select id="selectLatest" resultType="kr.or.ddit.domain.apt.board.ann.vo.AptAnnBoardPostVO">
        select p.board_no, ann_no, ttl, cn, reg_dttm
        from apt_ann_board_post p
        inner join apt_board_instance i on (p.board_no = i.board_no)
        where i.apt_cmplex_no = #{aptCmplexNo} and p.del_yn = 'N'
        order by p.reg_dttm desc
        fetch first #{limit} rows only
    </select>

</mapper>