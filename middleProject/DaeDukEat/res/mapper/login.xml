<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="login">

		<!-- 이용자 조회 로그인. 아이디랑 비밀번호 조회 -->	
		<select id="loginSelect" parameterType="uvo" resultType="uvo">
		SELECT 
            USER_ID as userId,      <!-- user_id → userId -->
            PASSWORD as password,    <!-- password → password -->
            NAME as name,            <!-- NAME → name  -->
            DIVISION as division,    <!-- division → division -->
            BLOCK_YN as blockYn,     <!-- block_yn → blockYn -->
            USE_YN as useYn          <!-- use_yn → useYn -->
        FROM USERS
        WHERE USER_ID = #{userId} 
          AND PASSWORD = #{password}
        <!--   AND USE_YN = 'Y'  -->
		</select>		
		
		<!-- 아이디 중복검사 -->
		<select id="idCheck" parameterType="string" resultType="string">
			select user_id from USERS
			where user_id = #{value}
		</select>
		
    	<!-- 아이디 찾기 	
		<select id="idSelect" parameterType="umvo" resultType="umvo">
			select u.name from USERS u,MEMBER m
			where u.user_id= m.user_id 
			and u.name = #{userName}
		</select> -->	
		
		<!-- 비밀번호 찾기 	
		<select id="pwSelect" parameterType="umvo" resultType="umvo">
			select u.user_id,m.user_mail from USERS u,MEMBER m
			where u.user_id= m.user_id 
			and u.user_id = #{userId}
			and m.user_mail = #{userMail}
		</select> -->
		
		<!-- 비밀번호 변경 -->
		<update id="updatePass" parameterType="umvo">
			update users
			set password = #{newPass}
			where user_id = #{userId}
		</update>
		
		
		
<!-- loginStore 섹션에 들어갈 객체 뽑아오는중 -->
		
<select id="selectStoreByUserId" parameterType="string" resultType="store">
    SELECT *
    FROM store
    WHERE user_id = #{userId}
</select>



</mapper>
