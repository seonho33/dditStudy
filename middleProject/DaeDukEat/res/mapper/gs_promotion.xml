<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.board.dao.IGsPromotionDao">

    <insert id="gsInsert" parameterType="kr.or.ddit.board.vo.GsPromotionVO">
        INSERT INTO GS_PROMOTION (
            GS_ID, PROD_NAME, ORIGINAL_PRICE, DISCOUNT_PRICE, 
            DISCOUNT_RATE, USER_ID, END_TIME, PROD_CATEGORY, PRODUCT_IMAGE_URL
        ) VALUES (
            SEQ_GS_PROMOTION.NEXTVAL, #{productName}, #{originalPrice}, #{discountPrice},
            #{discountRate}, #{userId}, #{endTime}, #{prodCategory}, #{productImageUrl}
        )
    </insert>

    <select id="selectGsPromotionList" resultType="kr.or.ddit.board.vo.GsPromotionVO">
        SELECT 
            GS_ID as gsId,
            PROD_NAME as productName,
            ORIGINAL_PRICE as originalPrice,
            DISCOUNT_PRICE as discountPrice,
            DISCOUNT_RATE as discountRate,
            USER_ID as userId,
            END_TIME as endTime,
            PROD_CATEGORY as prodCategory,
            PRODUCT_IMAGE_URL as productImageUrl
        FROM GS_PROMOTION
        ORDER BY GS_ID DESC
    </select>
    
    <delete id="gsDelete" parameterType="long">
        DELETE FROM GS_PROMOTION WHERE GS_ID = #{gsId}
    </delete>

</mapper>